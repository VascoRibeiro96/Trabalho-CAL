/*
 * Coordenada.cpp
 *
 *  Created on: 26/04/2016
 *      Author: Vasco
 */
/*
#include "Coordenada.h"
#include <Math.h>

using namespace std;

Coordenada::Coordenada()
{
	lat = 0;
	lon = 0;
}

Coordenada::Coordenada(double lat, double lon)
{
	this->lat = lat;
	this->lon = lon;
}

double Coordenada::dist(Coordenada c)
{

    double theta = this->lon - c.lon;
    double dist = sin(this->lat) * sin(c.lat) +  cos(this->lat) * cos(c.lat) * cos(theta);
    double dist = acos(dist);

    return dist;
}
*/
